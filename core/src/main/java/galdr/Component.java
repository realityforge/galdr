package galdr;

/**
 * Marker interface for component implementations.
 * A component is a passive container of data that is attached to an {@link Entity}.
 * A component is queried and/or mutated by {@link Processor} instances.
 */
public interface Component
{
}
